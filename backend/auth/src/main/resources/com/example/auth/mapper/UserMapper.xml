<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.auth.mapper.UserMapper">


    <insert id="insert">
        INSERT INTO tb_user (user_id,user_name,user_password)
        values(UUID(),#{userName},#{userPassword})
    </insert>

    <select id="getUserPassword" resultType="com.example.auth.pojo.po.User">
        SELECT user_id, user_name, user_password
        FROM tb_user
        WHERE user_name = #{userName}
    </select>
    
    <select id="getUserById" resultType="com.example.auth.pojo.po.User">
        SELECT  user_id, user_name, user_password
        FROM tb_user
        WHERE user_id = #{userId}
    </select>

    <select id="getUserByName" resultType="com.example.auth.pojo.po.User">
        SELECT  user_id, user_name, user_password
        FROM tb_user
        WHERE user_name = #{userName}
    </select>

    <select id="getUserByPassword" resultType="com.example.auth.pojo.po.User">
        SELECT  user_id, user_name, user_password
        FROM tb_user
        WHERE user_password= #{userPassword}
    </select>
</mapper>